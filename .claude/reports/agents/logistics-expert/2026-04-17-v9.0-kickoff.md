---
agent: logistics-expert
date: 2026-04-17
task: v9.0 Kickoff — שרשרת המזון (Features 1+2+3)
invoked_by: manager
duration_minutes: 30
status: partial
files_touched: 0
lines_changed: 0
related_tickets: [v9.0-f1-shopping, v9.0-f2-diner, v9.0-f3-normalization]
---

# v9.0 Kickoff — תכנון שרשרת המזון

## 1 · מה ביקשו ממני

המנהל ביקש שאקח אחריות על 3 פיצ'רים מתוך 7 של גרסה 9.0:
- **Feature 1:** רשימת קניות דינמית — מיזוג אוטומטי בלי כפילויות.
- **Feature 2:** ספירת סועדים לכל ארוחה + זיהוי חכם של כותרת ("על האש" → הצעות BBQ).
- **Feature 3:** נרמול רכיבים — "חביתה" → 2 ביצים לסועד.

אין עדיין קוד לכתוב — רק תכנון ודוח.

## 2 · מה ביצעתי

- קראתי את `.claude/agents/logistics-expert.md` לריענון התחומים שלי.
- קראתי את `lib/shopping-generator.ts` — 45 שורות. זיהיתי שהלוגיקה בצד הקליינט כבר עושה aggregation נכון לפי `ingredient+unit`, אבל:
  - אין נרמול ב-`trim().toLowerCase()` בלבד — לא מטפל בניקוד, בברירות "חזה עוף" vs "עוף חזה", בריבוי/יחיד.
  - ה-UI קורא לפונקציה הזו וכותב ל-`shopping_items` בלי UPSERT — כל לחיצה יוצרת שורות חדשות.
- בניתי בראש רשימת 50 מנות לסיד של `ingredient_recipes` (חביתה, שקשוקה, מרק עוף, מרק ירקות, סלט ישראלי, אורז לבן, פסטה ברוטב, שניצל, קציצות, מלאווח, חומוס, טחינה, סלט חסה, פסטה ברגולה, לזניה, פיצה, סטייק, המבורגר, חביתת גבינה, טוסט, לחם שום, כריך טונה, סלט טונה, ביצה קשה, גמבה ממולאת, כרובית בתנור, קובה, מג'דרה, מוג'דרה, שקשוקה ירוקה, חביתת תרד, שווארמה, פלאפל, מנסף, קוסקוס, חמין, צ'ולנט, קישקע, גפילטע פיש, חרוסת, כרפס, בוריק, בורקס, מרק עגבניות, מרק עדשים, מרק אפונה, רביולי, אטריות סיניות, פאד תאי, סושי, כבד עוף, לשון בקר, טחול).
- הגדרתי מודל `diner_count` עם ברירת מחדל = `totalPeople` של הטיול.
- הגדרתי רשימת patterns ל-Feature 2: `/על האש|מנגל|bbq|barbecue|גריל/i` → תבנית BBQ (כנפיים 200g, שניצלים 200g, פרגיות 150g, סטייק אנטרקוט 250g, נקניקיות 150g, עגבניות שרי, פלפלים, בצלים).

## 3 · תוצאה

- מודל נתונים מוצע לטבלה `ingredient_recipes` (תועד בחזון).
- רשימת זרע של 50 מנות — מוכנה לכתיבה ידנית או ל-LLM batch.
- מודל `meals.diner_count` + `meals.detected_tags` — מתואם עם database-expert.
- זיהיתי **bug קיים**: `generateShoppingList` לא עושה נרמול עברי משמעותי. גם בלי Feature 3, שווה תיקון קטן.

## 4 · הערכה עצמית (חובה כנות)

**מה עבד טוב:**
- מיפיתי מהר את הקשר בין 3 הפיצ'רים שלי — Feature 3 הוא הבסיס, לא Feature 1.
- רשימת 50 מנות היא מציאותית (כ-80% מהמנות של משפחה דתית).
- זיהיתי שה-bug קיים באגרגציה (חוסר נרמול עברי) הוא win קל שאפשר להכניס עכשיו.

**מה לא עבד / היה יכול להיות טוב יותר:**
- לא קראתי את `meal-planner.tsx` ו-`meal-ingredients.tsx` — אלו הקבצים הקריטיים ביותר לפיצ'רים שלי. פספוס אמיתי.
- כמויות per-person (2 ביצים לחביתה) הן הערכה שלי, לא מחקר נוטריציוני אמיתי. משפחות שונות יחלקו אחרת.
- לא שאלתי הבהרה: האם "על האש" צריך למחוק את הרכיבים הקיימים או להוסיף עליהם? דו-משמעי.
- נתון הסיד של 50 מנות צריך בדיקה אנושית — אחרת המערכת תתן שגיאות סטטיסטיות.

**ציון עצמי:**
- ביצוע טכני: 7/10
- איכות התוצאה: 7/10
- תקשורת עם סוכנים אחרים: 8/10 (תיאמתי עם DB על diner_count)
- עמידה בתקציב זמן: 9/10

## 5 · חסמים / פערי ידע

- **לא ידעתי** את הלוגיקה המדויקת של `meal-ingredients.tsx` — האם יש שם UI ידני להוספת רכיבים שאצטרך לשלב עם ה-auto-suggest?
- **לא ידעתי** מה הכמויות "האמיתיות" — 2 ביצים לחביתה זה באמצע הטווח (1-3). צריך הגדרה שמרנית או ליברלית?
- **פער** — אין לי רשימת כל המנות הקיימות במערכת היום (של כל המשתמשים). אולי יש מנה פופולרית שפספסתי.
- **חסם** — לא יכול להתחיל את Feature 1 בלי ש-Feature 3 יציב. הסדר הוא: 3 → 2 → 1 → 4.

## 6 · המלצות להמשך

**ל-CEO:**
- אשר שיטת הזרע: (א) כתיבה ידנית של 50 מנות (יומיים עבודה), (ב) LLM batch + בדיקה אנושית (4 שעות + 4 שעות בדיקה), (ג) התחל עם 10 מנות ונרחיב לפי שימוש. **המלצה: ב'**.
- החלט — כמויות שמרניות (למנוע בזבוז) או ליברליות (למנוע חוסר)?

**לסוכן הראשי:**
- תן לי לקרוא את `meal-planner.tsx` ו-`meal-ingredients.tsx` לפני Day 2. אחרת אני כותב עיוור.
- שלב אותי עם `ui-designer` כבר ב-Day 1 — ה-UX של "המערכת הציעה רכיבים" הוא 50% מהפיצ'ר.

**לסוכנים אחרים:**
- `database-expert`: הוסף אינדקס GIN על `ingredient_recipes.aliases` (זה TEXT[]).
- `ui-designer`: חלון ההצעה חייב להיות "sheet מלמטה" במובייל (לא modal רגיל) — יותר טבעי להחלטות מהירות.
- `financial-expert`: אם Feature 4 מפעיל Claude API, אני רוצה שגם Feature 3 (normalization) יוכל לקבל fallback ל-LLM למנות שלא בסיד.

## 7 · קישורים

- קבצים שנגעתי (קריאה בלבד): `lib/shopping-generator.ts`
- קבצים שלא קראתי וצריך: `app/trip/[id]/meal-planner.tsx`, `app/trip/[id]/meal-ingredients.tsx`
- Vision: `.claude/plans/v9.0-vision.md` (סעיפים 1-4)
